#ifndef MATH_BUILTIN_H
#define MATH_BUILTIN_H
#include <cmath>
namespace math {
  const double PI = acos(1.0);
  const double E = exp(1.0);
  const double PHI = (sqrt(5) - 1) / 2;
} // namespace math
#endif
